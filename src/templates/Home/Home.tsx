import React from 'react';

import styles from './Home.module.scss';

const Home: React.FC = () => {
  return (
    <main className={styles.wrapper}>
        Test content
    </main>
  );
};

export default Home;
